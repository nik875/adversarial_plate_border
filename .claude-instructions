# Claude Code Custom Instructions for Adversarial Plate Repository

## Important Constraints
- **CANNOT run code locally on this machine** - All work must be pushed to the repository
- All changes MUST be committed and pushed to the repository immediately after completion
- Do not run tests, training, or compute-intensive operations locally

## Workflow
1. Make code changes
2. Commit with descriptive message (include Co-Authored-By footer)
3. Push to the appropriate branch (usually `foundationmodel`)
4. User will pull and run on their compute machine using the provided scripts

## For the User
Use the provided `run-on-compute.sh` script to pull the latest changes and execute them on your compute machine.
